# 引き継ぎメモ（旅のしおり）

新しいセッションで作業を再開するとき、まずこれを読めば文脈が復元できるようにまとめたもの。
最終更新: 2026-08-08 時点の `main`（`e9e23a8`）

---

## 1. これは何か

旅行の行程表（しおり）を作って共有するWebアプリ。**ビルド不要の静的ファイル構成**で、
GitHub Pages で公開している。

- **公開URL**: https://krgok.github.io/tabi-no-shiori/
- **リポジトリ**: https://github.com/krgok/tabi-no-shiori （public）
- **仕様書**: `SPEC.md`（機能ごとに番号付きの節。実装の判断理由もここに書いてある）

---

## 2. 最初にやること

```bash
cd "C:\Users\hiro\Claude\Projects\旅のしおり"
npm test        # 全16ファイル・782アサーション。1つでも落ちたら非0で終了する
```

**変更したら必ず `npm test` を通してからコミットすること。** push すると GitHub Actions が
同じテストを回し、Green になってから GitHub Pages に反映される（1〜2分）。

ローカルで動かす場合は `サーバー起動.bat` をダブルクリック（= `python -m http.server 8934`）。
`file://` で直接開くと Service Worker と一部機能が動かない。

---

## 3. ファイル構成（重要: 読み込み順に依存する）

`app.js`（7,300行）は **`js/` 配下の10ファイルに分割済み**。元は1つの巨大IIFEだったものを
解いてグローバルスコープの通常スクリプトにしてあるので、**読み込み順が変わると壊れる**。

| ファイル | 役割 |
|---|---|
| `js/00-core.js` | 定数・状態変数・ユーティリティ |
| `js/10-gmaps.js` | Google Maps リンクの座標抽出・生成 |
| `js/20-data.js` | localStorage・正規化・**公開用サニタイズ**・共同編集マージ |
| `js/30-cloud.js` | Firebase 認証・クラウド同期・共有/編集リンクの発行 |
| `js/40-ui.js` | DOM・描画・時差・チェックリスト・項目CRUD・ドラッグ |
| `js/50-route.js` | ルート計算・位置解決・多言語名取得・地図 |
| `js/60-share.js` | 複数しおり・共有/公開/編集リンクの受信 |
| `js/70-csv.js` | CSV入出力・全データのバックアップ/復元 |
| `js/80-print.js` | PDF出力・モーダル・トースト |
| `js/90-main.js` | イベント登録・初期化・PWA登録 |

**ファイルを追加・改名したら、次の3箇所すべてを同じ順序で更新すること:**
1. `index.html` の `<script>` タグ
2. `tests/harness.js`（テスト用のローダー）
3. `sw.js` の `PRECACHE_URLS`（漏れるとオフライン起動が壊れる）

→ 3の漏れは `tests/test-pwa.js` がCIで検知する。

その他: `i18n.js`（4言語の辞書）、`styles.css`、`vendor/`（Leaflet・Firebase SDKを同梱）、
`icons/`、`manifest.webmanifest`、`sw.js`

---

## 4. 触るとき特に注意すること

### 4-1. 🔒 非公開マークとサニタイズ（最重要）
`sanitizeTripForPublic()`（`js/20-data.js`）が「共有してよいデータ」を作る中核。
**共有・公開・編集リンクに流すデータは必ずこれを通す。** 非公開の項目・日・メモ・リストは
フラグごと削除され、公開データに痕跡が残らない。翻訳文（`noteNames`）も同時に消すこと
（過去にここが漏れて、非公開メモの翻訳だけ流出しかけた）。

### 4-2. 3種類の共有リンクの違い
| リンク | 形式 | 相手の編集 | オーナーの変更が届くか |
|---|---|---|---|
| 共有リンク | `#d=`（URL埋め込み） | できる（相手の手元コピー） | 届かない |
| 共有リンク | `#p=`（クラウド） | できない（閲覧専用） | **届かない**（作成時点で固定。🔄更新ボタンで手動反映） |
| 編集リンク | `#e=`（クラウド） | できる | **届く**（双方向リアルタイム同期） |

UI上は `#d=` と `#p=` を「共有」に統合済み（ログイン有無で自動切替）。
`#e=` は別建てで、ユーザーが実際に家族へ配布中。**`#e=` の挙動は壊さないこと。**

### 4-3. 閲覧モード（`viewOnly`）と共同編集モード（`collabMode`）
- `viewOnly`（`#p=`）: **localStorage を絶対に汚染しない。** `saveState()` の冒頭で return する多層ガード
- `collabMode`（`#e=`）: 編集は可能だが、自分の `trips` には書かず `editTrips` へ push する

### 4-4. 過去に何度も踏んだ罠
- **`requestAnimationFrame` / `ResizeObserver` はこの環境で発火しないことがある。** 地図の初期化と
  モーダルのサイズ保存で実害が出た。フレーム依存APIに頼らず同期実行するか、`pointerup` 等で代替する
- **textarea の高さ自動調整は、幅が確定する前に測ると桁違いの値になる。** `autoGrowNote()` は
  `offsetWidth` を読んで強制 reflow してから `scrollHeight` を測っている
- **CSVはExcelで編集されるとカンマ埋めになる。** 空行が `,,,,,,` に、タイトル行が
  `title,名前,,,,,,` になる。判定を「ちょうどN個」にすると壊れる（2回踏んだ）
- **`<input type="date">` は `YYYY-MM-DD` しか受け付けない。** `2026/7/24` を渡すと空欄になり
  「日付が消えた」ように見える。`normalizeDateStr()` を通すこと
- **JS/CSSを変更したら `sw.js` の `CACHE_VERSION` を必ず上げること。** アプリ本体はキャッシュ優先で
  配られるため、上げ忘れると既存ユーザーには新機能が一切届かない（デプロイ済みなのに
  「ボタンが見えない」報告になる。実績記録の初回リリースで踏んだ）。**同時に `js/00-core.js` の
  `APP_VERSION` も同じ値へ上げること。** ⚙️設定モーダルに表示され、ユーザー自身が「更新が端末に
  届いたか」を確認できる（サポートの切り分け用）。2つの値が一致しているかは `tests/test-pwa.js` が
  CIで検知する

---

## 5. 外部サービスの設定（すべて設定済み）

### Firebase（プロジェクト `tabi-no-shiori-71b24`）
- Googleログイン有効化済み。承認済みドメインに `krgok.github.io` 登録済み
- Firestore セキュリティルール適用済み。**一覧取得(list)は全コレクションで禁止**しており、
  `trips` は本人のみ、`publicTrips`/`editTrips` はURLを知る人だけが個別取得できる
- **Sparkプラン（無料・課金なし）**
- `firebaseConfig` は `js/00-core.js` にハードコード（公開前提の値なので問題ない）

### Google Maps Platform（別プロジェクト `gen-lang-client-0907226733`）
- APIキーはコードに無く、**⚙️設定からユーザーが入力してブラウザ内にのみ保存**される。
  共有リンク・CSV・バックアップには一切含まれない
- キー `travel_itinerary` は Routes / Places (New) / Cloud Translation に制限済み、
  リファラー制限は `localhost:8934/*` と `https://krgok.github.io/*`
- **キーはURLごとに別保存。** localhost で設定しても公開URLには引き継がれない（サポート時の頻出質問）

---

## 6. テストについて

`tests/` に16ファイル・782アサーション。`npm test` で一括実行、`.github/workflows/test.yml` でCI。

- `harness.js` が jsdom で実物の `index.html` + `i18n.js` + `js/*.js` を読み込み、
  Firestore をスタブして本物のDOMイベントで検証する（モックではなく実コードを動かす）
- `tests/fixtures/import-sample.csv` は実ユーザーCSVと同じ構造の**合成データ**。
  個人の旅行データはリポジトリに置かない方針
- 新機能を足したら対応するテストも足し、`tests/run-all.js` のリストに登録すること

---

## 7. 直近の作業（2026-08 の大きな改善）

ユーザー依頼「A-D全て実装」に対して5フェーズで実施、すべて完了・デプロイ済み:

1. テストをリポジトリに取込み、GitHub Actions でCI化
2. **しおり削除時に公開リンクも削除**（プライバシーバグ修正）／削除の「元に戻す」／全データのバックアップ・復元
3. 出発カウントダウン／しおり複製／↑↓と日跨ぎ移動／☁同期状態表示／共同編集の更新通知
4. **PWA化**（オフライン起動・ホーム画面追加）
5. `app.js` を10ファイルに分割

その後、別セッションで `e9e23a8`（項目の別日コピー、ルート計算での固定時刻の保持）が追加されている。
さらにその後、実績記録フェーズ1（✓ボタンで到着時刻とGPSを記録し予定との差を表示。座標は共有・編集リンクから除外）を追加。
さらにその後、実績記録の端末間同期バグを修正（`actualAt` を追加し、`mergeTripActuals` で項目単位マージ。SPEC 23参照。CACHE_VERSION は v5 に更新済み）。
さらにその後、上記マージ修正が「クラウド採用方向」にしか効いておらず、「ローカルをアップロードする方向」では実績が丸ごと消え得た残課題を修正（`computeTripsMergePlan` の `uploads` を `{entry, cloudDoc}` にし、アップロード前にも `mergeTripActuals` を適用。SPEC 23参照。CACHE_VERSION は v7 に更新済み）。
さらにその後、実績と同じ原因（丸ごと最後の書き込み優先）で項目のメモ（note）も端末間同期で消える不具合を修正（`noteAt` を追加し、`mergeTripActuals`/`mergeDayItems` に note/noteAt の組を項目単位でマージ。notePriv保護は維持。SPEC 23参照。CACHE_VERSION/APP_VERSION は v8 に更新済み）。

---

## 8. 未着手・今後の候補

- **実機スマホでの確認が未実施**: ドラッグ並べ替え、PWAのホーム画面追加、オフライン起動
  （いずれもデスクトップとjsdomでは検証済み）
- 共同編集の競合は「最後の書き込み優先」。家族利用なら実用上問題ないが、同時編集が増えるなら要検討
- ユーザーが挙げていた元ネタアプリの機能で未実装のもの: 旅行のアーカイブ済み一覧の充実など
- `js/40-ui.js` が約2,000行とやや大きい。さらに分けるならここ

---

## 9. ユーザーとのやりとりで踏まえること

- 実装は **Sonnet 5 のサブエージェントに委任**し、メインは指示とレビューに徹する方針
  （`~/.claude/.../memory/model-preference-sonnet.md` に記録済み）
- 報告は日本語。技術用語は噛み砕く（「サニタイズ」より「非公開部分を除いたコピー」など）
- **不具合報告を受けたら、まず実データ・実ファイルで再現してから直す。** 推測で直そうとして
  外したことが複数回ある（「日付が消えた」は実は表示だけの問題、など）
